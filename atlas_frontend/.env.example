# Optional: copy to .env.local. See ../docs/configuration.md.
# VITE_* values are public browser configuration. Never put secrets here.

# Empty uses same-origin /api requests (recommended).
VITE_API_BASE_URL=
