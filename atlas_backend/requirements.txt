# Runtime
anthropic==0.124.0
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.52.4
pydantic==2.12.3
pydantic-settings==2.14.2
requests==2.34.2
python-dotenv==1.2.3
pyyaml==6.0.3

# Knowledge base: reading uploaded documents, and accepting the upload.
# Embeddings are produced by the LOCAL Ollama runtime, so there is no API
# client here and nothing to pay for.
pypdf==6.16.1
python-docx==1.1.2
python-multipart==0.0.31

# Optional — only for DATA_SOURCE=sql against a networked database. The SQL
# adapter uses the stdlib DB-API, so install just the driver you need:
#   PostgreSQL / TimescaleDB:  psycopg[binary]   (or psycopg2-binary)
#   MySQL / MariaDB:           pymysql
# SQLite needs nothing extra (stdlib sqlite3).

# Development
pytest==9.0.3
httpx==0.28.1
ruff==0.14.2
